# Template of Webpack with React
A template of webpack config using React and styled-components

## Scripts
### `npm start`
Start a developing server at your `localhost:3000`

### `npm run build`
Build the proyect for production

